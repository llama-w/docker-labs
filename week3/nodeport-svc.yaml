apiVersion: v1
kind: Service
metadata:
  name: web-nodeport
spec:
  type: NodePort
  selector:
    app: web          # web-deploy.yaml의 라벨과 일치
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080  # 30000-32767 범위, 고정하면 curl 편함
